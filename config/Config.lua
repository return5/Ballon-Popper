local config = {
	height = 500,
	width = 500,
	balloonSpeedMin = 100,
	balloonSpeedMax = 200,
	characterSpeed = 220
}

config.balloonStartY = config.height - 10
config.characterStartY = config.balloonStartY - 10

return config