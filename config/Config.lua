local config = {
	height = 500,
	width = 600,
	balloonSpeedMin = 100,
	balloonSpeedMax = 200,
	characterSpeed = 270
}

config.balloonStartY = config.height - 10
config.characterStartY = config.balloonStartY - 10

return config